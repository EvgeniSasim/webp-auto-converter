name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: PHP syntax check
        run: |
          php -l sasim-webp-auto-converter/sasim-webp-auto-converter.php
          php -l sasim-webp-auto-converter/uninstall.php
          php -l sasim-webp-auto-converter/includes/image-helpers.php
          php -l sasim-webp-auto-converter/includes/auto-output.php

      - name: Verify readme version match
        run: |
          PHP_VERSION=$(grep 'Version:' sasim-webp-auto-converter/sasim-webp-auto-converter.php | awk '{print $NF}')
          README_TAG=$(grep 'Stable tag:' sasim-webp-auto-converter/readme.txt | awk '{print $NF}')
          test "${PHP_VERSION}" = "${README_TAG}"

      - name: Build release zip
        run: bash scripts/build-release.sh

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: sasim-webp-auto-converter-zip
          path: build/sasim-webp-auto-converter.zip
